import 'package:flutter/material.dart';

class ThreeDModel extends StatelessWidget {
  const ThreeDModel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('3D model')),
      body: const Center(child: Text('3 D model')),
    );
  }
}